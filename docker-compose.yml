services:
  piped:
    image: 1337kavin/piped:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./config.properties:/app/config.properties
    # No 'depends_on' or 'postgres' service, because you use managed DB

# Remove the postgres service completely!
